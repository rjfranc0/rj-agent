# Target: react-native (stub)

For a React Native app in the workspace. Backlog — no immediate project needs this.

**Known shape** (for expansion): similar in spirit to `tauri-release` (build matrix, store-specific signing — App Store / Play Store, not GitHub Releases), but with EAS (Expo Application Services) or Fastlane as the likely tooling layer rather than a hand-rolled matrix.

Expand on first real use.
